1. Скрипт создания БД спортивных залов
PASS

2. Если загрузить CSV с 1000 записей под именем MOCK_DATA, то скрипт для генерации будет выглядеть следующим образом:
SELECT CONCAT(MD1.first_name, ' ', MD2.last_name)
FROM MOCK_DATA MD1, MOCK_DATA MD2

Останется обернуть его в INSERT для вставки в наши таблицы.

3. Можем использоваться когда требуется переиспользование кода, сокрытие сложной реализации/кода.
В отличие от подзапроса структуру с WITH можно переиспользовать в коде целиком вместо того, чтобы писать дублирующиеся строки текста.